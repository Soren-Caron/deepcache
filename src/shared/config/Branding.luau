--!strict
--[[ Pure data. The working title lives here and nowhere else, so renaming
	the project is a one-line change. ]]

return {
	gameName = "DEEPCACHE",
	directorName = "OVERSEER",
	tagline = "Twelve minutes. Everything you carry is at risk.",
	runLengthSeconds = 720,
	squadSizeDefault = 3,
	squadSizeMax = 4,
}
